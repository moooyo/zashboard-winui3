<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Zashboard.App.Controls.EmptyState"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="240"
    d:DesignWidth="480"
    mc:Ignorable="d">

    <Grid MinHeight="180" Padding="24">
        <StackPanel
            MaxWidth="420"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            AutomationProperties.AccessibilityView="Content"
            AutomationProperties.HelpText="{x:Bind Description, Mode=OneWay}"
            AutomationProperties.LiveSetting="Polite"
            AutomationProperties.Name="{x:Bind StateTitle, Mode=OneWay}"
            Spacing="8">
            <FontIcon
                HorizontalAlignment="Center"
                AutomationProperties.AccessibilityView="Raw"
                FontSize="30"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Glyph="{x:Bind IconGlyph, Mode=OneWay}" />
            <TextBlock
                HorizontalAlignment="Center"
                AutomationProperties.AccessibilityView="Raw"
                FontFamily="{StaticResource AppDisplayFontFamily}"
                FontSize="17"
                FontWeight="SemiBold"
                Text="{x:Bind StateTitle, Mode=OneWay}"
                TextAlignment="Center"
                TextWrapping="Wrap" />
            <TextBlock
                HorizontalAlignment="Center"
                AutomationProperties.AccessibilityView="Raw"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="{x:Bind Description, Mode=OneWay}"
                TextAlignment="Center"
                TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</UserControl>
